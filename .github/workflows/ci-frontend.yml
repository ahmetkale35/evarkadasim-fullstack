name: CI — Frontend

on:
  push:
    paths:
      - 'frontend/**'
      - '.github/workflows/ci-frontend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'

# Frontend yeniden kurulunca:
# 1. working-directory → frontend/<klasör-adı> güncelle
# 2. cache-dependency-path → aynı klasör/package-lock.json güncelle
# 3. if: false satırını sil

jobs:
  type-check-and-lint:
    name: TypeScript & Lint (Expo)
    runs-on: ubuntu-latest
    if: false
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: TypeScript type check
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Lint
        working-directory: frontend
        run: npm run lint
